# Quick Setup Guide

After cloning this repo:

```bash
cd esphome-devices

# 1. Run setup (installs pre-commit hook)
./setup.sh

# 2. Test validation works
./validate-all.sh
```

That's it! Now when you `git commit`, configs are validated automatically.
